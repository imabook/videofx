-- un poco feo la verda no se si renta xd
-- return { table.unpack(require "edit.algorithms.interpolations"), table.unpack(require "edit.algorithms.random") }
local interpolations = require "edit.algorithms.interpolations"
return interpolations
